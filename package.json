{
  "name": "engducationgroup",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:web": "pnpm --filter web dev",
    "db:push": "pnpm --filter @engducationgroup/db db:push",
    "db:studio": "pnpm --filter @engducationgroup/db db:studio",
    "db:generate": "pnpm --filter @engducationgroup/db db:generate",
    "db:migrate": "pnpm --filter @engducationgroup/db db:migrate"
  },
  "dependencies": {
    "@engducationgroup/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@engducationgroup/config": "workspace:*",
    "@types/node": "^22.13.14",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.28.2"
}
